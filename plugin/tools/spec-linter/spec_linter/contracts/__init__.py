"""Contract implementations — policy packaged behind the `Contract` protocol.

Three sources: authored as code (`agent_spec`), loaded as data (`sdd_phase`),
derived from a spec instance (`instance`).
"""
